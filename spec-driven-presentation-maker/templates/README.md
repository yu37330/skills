# Templates

Place your `.pptx` template files here.

spec-driven-presentation-maker uses these templates as the base for slide generation. Run `analyze-template` to inspect layouts, theme colors, and fonts.

```bash
python scripts/pptx_builder.py analyze-template your-template.pptx
```
