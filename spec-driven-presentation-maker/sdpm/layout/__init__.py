# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: MIT-0
"""Layout engine: compute coordinates from logical structure JSON.

The engine is split into cohesive submodules; this package re-exports
every name so existing imports (``from sdpm.layout import X`` and the
``from . import X`` used by render/metrics/graph) keep working.
"""

from .ordering import (  # noqa: F401
    _REFLOW_MAX_LEAVES,
    _collect_leaf_ids,
    _column_partitions,
    _compute_global_peer_positions,
    _compute_peer_positions,
    _count_crossings_for_mixed_order,
    _count_crossings_for_order,
    _defect_tuple,
    _find_min_crossing_order,
    _find_min_crossing_order_mixed,
    _find_tile_pools,
    _flatten_ids_from_root,
    _heuristic_sort_key,
    _heuristic_sort_key_mixed,
    _is_tile_column,
    _optimize_group_order,
    _peer_detour_cost,
    _promote_branch_nodes,
    _reflow_tile_pools,
    _tag_manual_branch_anchors,
    optimize_order,
)
from .placement import (  # noqa: F401
    _align_branch_lane_anchors,
    _align_corresponding_leaves_x,
    _align_corresponding_leaves_y,
    _align_leaves_to_sibling_centers,
    _align_leaves_to_sibling_centers_h,
    _cluster_groups_by_axis_overlap,
    _collect_horizontal_groups,
    _collect_vertical_groups,
    _find_leaf_centers_x,
    _find_leaf_centers_y,
    _get_direct_leaves,
    _layout_collect,
    _layout_scale,
    _layout_translate,
    _ranges_overlap,
    _recompute_group_bbox,
    _reflow_children_along_axis,
    cancel_cross_axis_squash,
    measure_natural_child_sizes,
)
from .routing import (  # noqa: F401
    _FAN_SPREAD_LIMIT,
    _GROUP_BUS_LANE_GAP,
    _GROUP_BUS_PORT_GAP,
    _PORT_EPS,
    _UTURN_CLEAR,
    _align_fan_bends,
    _align_group_bus,
    _count_port_crossings,
    _heuristic_port_sort,
    _layout_route_connections,
    _optimize_port_order,
    _recenter_ports,
    _safe_separate_bends,
    _spread_overlapping_bends,
    _straighten_group_edges,
    _uturn_group_endpoint_edges,
)
from .geometry import (  # noqa: F401
    _GROUP_FRAME_INSET,
    _PIERCE_INSET,
    _PIERCE_WEIGHT,
    _count_all_crossings,
    _count_backwards,
    _count_group_pierces,
    _count_node_pierces,
    _edge_free_bend,
    _find_crossing_pairs,
    _find_first_crossing,
    _is_fan_trunk_t_junction,
    _perp_touch,
    _port_side,
    _seg_crosses_box,
    _seg_pierces_node,
    _segments_cross,
    _segments_intersect,
    _segments_overlap_collinear,
)
from .refine import (  # noqa: F401
    _BEND_CANDIDATES,
    _BEND_OPT_INSET,
    _BEND_OPT_PASSES,
    _BEND_OPT_STEP,
    _DEFECT_W,
    _DETOUR_FACE_MARGIN,
    _DETOUR_PASSES,
    _FAN_BEND_MARGIN,
    _FAN_GROUP_CLEARANCE,
    _JOG_ARM_STEP,
    _MAX_RESOLVE_ITERATIONS,
    _MIN_BEND_SEPARATION,
    _PORT_TRIALS,
    _RESELECT_CROSS_SLACK,
    _SIDE_RESELECT_PASSES,
    _W_GROUP_PIERCE_ENGINE,
    _align_same_source_bends,
    _apply_bend_shift_x,
    _apply_bend_shift_y,
    _candidate_side_pairs,
    _defect_weight,
    _detour_around_pierces,
    _edge_backwards,
    _edge_pierces,
    _enclosing_framed_group,
    _entry_exit_ok,
    _fan_side_vote,
    _is_axis_aligned,
    _jog_candidates,
    _min_bend_separation,
    _normalize_path,
    _optimize_bends,
    _optimize_jog_arm,
    _optimize_single_bend,
    _path_stability,
    _push_trunk_outside_group,
    _reselect_sides,
    _resolve_crossing_search,
    _rewrite_fan,
    _separate_close_bends,
    _separate_close_horizontal_segments,
    _update_bend_x,
)
from .model import (  # noqa: F401
    MIN_BEND_MARGIN,
    OBSTACLE_MARGIN,
    SNAP_THRESHOLD,
    _DETOUR_MARGIN,
    _auto_sides,
    _calc_bend,
    _detour_path,
    _elbow_path,
    _find_endpoint,
    _find_group,
    _find_group_for,
    _find_node,
    _fix_bends_inside_nodes,
    _group_member_ids,
    _group_qualified_id,
    _port_point,
    box_to_elements,
)
